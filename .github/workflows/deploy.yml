name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  DEPLOY_PATH: /root/sns-marketing

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Build App and Python Worker in parallel
  build-app:
    name: Build App Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for App
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push App image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.prod
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NODE_ENV=production
            VITE_APP_ID=${{ secrets.VITE_APP_ID }}
            VITE_OAUTH_PORTAL_URL=${{ secrets.VITE_OAUTH_PORTAL_URL }}

  build-python:
    name: Build Python Worker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Python Worker
        id: meta-python
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-python
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Python Worker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.python
          push: true
          tags: ${{ steps.meta-python.outputs.tags }}
          labels: ${{ steps.meta-python.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: [build-app, build-python]
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY_B64 }}" | base64 -d > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts
          echo "=== Key file info ==="
          ls -la ~/.ssh/deploy_key
          head -1 ~/.ssh/deploy_key
          tail -1 ~/.ssh/deploy_key
          echo "=== Testing SSH connection ==="
          ssh -v -o BatchMode=yes -o ConnectTimeout=10 -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "echo SSH OK" || echo "SSH test failed"

      - name: Create deployment directory
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'ENDSSH'
            set -e
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            DEPLOY_DIR="${{ env.DEPLOY_PATH }}/deployments/${TIMESTAMP}"

            # Create new deployment directory
            mkdir -p "$DEPLOY_DIR"

            # Save current symlink target for potential rollback
            if [ -L "${{ env.DEPLOY_PATH }}/current" ]; then
              CURRENT=$(readlink -f "${{ env.DEPLOY_PATH }}/current")
              echo "$CURRENT" > "${{ env.DEPLOY_PATH }}/previous"
            fi

            echo "$DEPLOY_DIR" > "${{ env.DEPLOY_PATH }}/new_deploy_dir"
          ENDSSH

      - name: Transfer docker-compose.prod.yml
        run: |
          DEPLOY_DIR=$(ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "cat ${{ env.DEPLOY_PATH }}/new_deploy_dir")
          scp -i ~/.ssh/deploy_key docker-compose.prod.yml ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:$DEPLOY_DIR/
          scp -i ~/.ssh/deploy_key scripts/deploy/health-check.sh ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:$DEPLOY_DIR/

      - name: Deploy new version
        env:
          IMAGE_TAG: ${{ needs.build-app.outputs.image_tag }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "echo '${{ secrets.GITHUB_TOKEN }}' | docker login ghcr.io -u ${{ github.actor }} --password-stdin"
          ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << ENDSSH
            set -e
            DEPLOY_DIR=\$(cat ${{ env.DEPLOY_PATH }}/new_deploy_dir)
            cd "\$DEPLOY_DIR"

            # Create .env file with secrets
            cat > .env << 'EOF'
          DATABASE_URL=${{ secrets.DATABASE_URL }}
          REDIS_URL=${{ secrets.REDIS_URL }}
          MYSQL_ROOT_PASSWORD=${{ secrets.MYSQL_ROOT_PASSWORD }}
          MYSQL_USER=${{ secrets.MYSQL_USER }}
          MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}
          DUOPLUS_API_KEY=${{ secrets.DUOPLUS_API_KEY }}
          OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          VITE_APP_ID=${{ secrets.VITE_APP_ID }}
          VITE_OAUTH_PORTAL_URL=${{ secrets.VITE_OAUTH_PORTAL_URL }}
          OAUTH_SERVER_URL=${{ secrets.OAUTH_SERVER_URL }}
          PRODUCTION_URL=${{ secrets.PRODUCTION_URL }}
          GITHUB_REPOSITORY=${{ github.repository }}
          IMAGE_TAG=${IMAGE_TAG}
          EOF

            # Pull new images
            docker compose -p sns-marketing -f docker-compose.prod.yml pull

            # Stop ALL containers using port 3000 (handle legacy deployments with different project names)
            echo "Stopping all containers using port 3000..."
            docker ps --filter "publish=3000" -q | xargs -r docker stop 2>/dev/null || true
            docker ps -a --filter "publish=3000" -q | xargs -r docker rm -f 2>/dev/null || true

            # Also stop any containers from old timestamp-based project names
            docker ps -a --format '{{.Names}}' | grep -E '^[0-9]{8}_[0-9]{6}' | xargs -r docker rm -f 2>/dev/null || true

            # Stop current sns-marketing project if running
            docker compose -p sns-marketing -f docker-compose.prod.yml down --remove-orphans 2>/dev/null || true

            # Start new containers with fixed project name
            docker compose -p sns-marketing -f docker-compose.prod.yml up -d

            # Wait for database to be healthy (uses docker-compose healthcheck)
            echo "Waiting for database to be ready..."
            timeout 60 bash -c 'until docker compose -p sns-marketing -f docker-compose.prod.yml ps db | grep -q "healthy"; do echo "Waiting for database..."; sleep 5; done' || echo "DB wait timeout"
            echo "✅ Database is ready"

            # Run database migrations (as root to avoid permission issues)
            echo "Running database migrations..."
            if docker compose -p sns-marketing -f docker-compose.prod.yml exec -T -u root app pnpm db:push; then
              echo "✅ Database migration completed successfully"
            else
              echo "⚠️ Database migration failed - check logs above"
              # Continue deployment but log the failure
            fi

            # Update symlink
            ln -sfn "\$DEPLOY_DIR" "${{ env.DEPLOY_PATH }}/current"

            echo "Deployment directory: \$DEPLOY_DIR"
          ENDSSH

      - name: Health check
        id: health_check
        continue-on-error: true
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'ENDSSH'
            set -e
            DEPLOY_DIR=$(cat ${{ env.DEPLOY_PATH }}/new_deploy_dir)
            cd "$DEPLOY_DIR"
            chmod +x health-check.sh
            ./health-check.sh "http://localhost:3000/api/trpc/health" 30 5
          ENDSSH

      - name: Rollback on failure
        if: steps.health_check.outcome == 'failure'
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'ENDSSH'
            set -e
            echo "Health check failed! Rolling back..."

            if [ -f "${{ env.DEPLOY_PATH }}/previous" ]; then
              PREVIOUS=$(cat "${{ env.DEPLOY_PATH }}/previous")

              if [ -d "$PREVIOUS" ]; then
                echo "Rolling back to: $PREVIOUS"

                # Stop failed deployment
                FAILED_DIR=$(cat "${{ env.DEPLOY_PATH }}/new_deploy_dir")
                cd "$FAILED_DIR"
                docker compose -p sns-marketing -f docker-compose.prod.yml down --remove-orphans 2>/dev/null || true

                # Start previous version
                cd "$PREVIOUS"
                docker compose -p sns-marketing -f docker-compose.prod.yml up -d

                # Update symlink
                ln -sfn "$PREVIOUS" "${{ env.DEPLOY_PATH }}/current"

                echo "Rollback complete!"
              else
                echo "Previous deployment directory not found: $PREVIOUS"
                exit 1
              fi
            else
              echo "No previous deployment found for rollback!"
              exit 1
            fi
          ENDSSH

      - name: Fail if health check failed
        if: steps.health_check.outcome == 'failure'
        run: |
          echo "Deployment failed health check and was rolled back!"
          exit 1

      - name: Cleanup old deployments
        if: steps.health_check.outcome == 'success'
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'ENDSSH'
            set -e
            DEPLOYMENTS_DIR="${{ env.DEPLOY_PATH }}/deployments"

            # Keep only the 3 most recent deployments
            cd "$DEPLOYMENTS_DIR"
            ls -dt */ | tail -n +4 | xargs -r rm -rf

            # Prune unused Docker images
            docker image prune -af --filter "until=168h" 2>/dev/null || true

            echo "Cleanup complete!"
          ENDSSH

      - name: Deployment Summary
        if: success()
        run: |
          echo "## Deployment Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Tag:** ${{ needs.build-app.outputs.image_tag }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment:** production" >> $GITHUB_STEP_SUMMARY
          echo "- **Deployed at:** $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
